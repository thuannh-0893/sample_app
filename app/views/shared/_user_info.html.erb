<%= link_to gravatar_for(current_user, size: Settings.gravatar_for.size), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t(".view_profile"), current_user %></span>
<span><%= t(".microposts_count", m_count: current_user.microposts.count)%></span>
